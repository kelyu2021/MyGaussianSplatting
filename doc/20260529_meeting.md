1. how depth is calculated?
	invdepth_disp = alpha * (1.0 / pkg["depth"].clamp(min=1e-6))
    how does the sky at MAXDEPTh contribute? Does it contribute at all? Sky should be treated as alpha=1, depth=MAXDEPTH

    
a. reproduce:  Difix3D and 3DGS-Enhancer
b. render off-path video
c. add normal loss to our code